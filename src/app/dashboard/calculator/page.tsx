import { CalculatorClient } from "@/components/calculator/CalculatorClient";

export const metadata = { title: "Risk Calculator" };

export default function CalculatorPage() {
  return <CalculatorClient />;
}
